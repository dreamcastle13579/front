import React from "react";

import "./Interpret.scss";

import { Layout, Main } from "../../frame";

const Interpret = () => {
  return (
    <Layout>
      <Main>interpret</Main>
    </Layout>
  );
};

export default Interpret;
