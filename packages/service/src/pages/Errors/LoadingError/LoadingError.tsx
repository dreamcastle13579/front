import React from "react";

import "./LoadingError.scss";

import { Layout, Main } from "../../../frame";

const LoadingError = () => {
  return (
    <Layout>
      <Main>loading error</Main>
    </Layout>
  );
};

export default LoadingError;
